/// out: "Hello, world!"

struct File extern("FILE")

def fopen(s: string, p: string): &File extern
def fwrite(s: string, a: i32, b: i32, f: &File): i32 extern

def main() {
    let f = fopen("/dev/stdout", "w");
    fwrite("Hello, world!", 1, 13, f);
    return 0
}
